{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build:server",
  "outputDirectory": "packages/server/build",
  "builds": [
    {
      "src": "packages/server/build/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["packages/server/build/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "packages/server/build/server.js"
    }
  ],
  "build": {
    "env": {
      "NODE_VERSION": "18.x"
    }
  }
}